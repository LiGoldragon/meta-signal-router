# Working in meta-signal-router

Read `ARCHITECTURE.md` and the ordinary Router contract `signal-router` before
changing this contract. `ethos/signal.ethos` is the authority; the Rust in
`src/generated/` is generated and asserted fresh by `build.rs` — never edit it
by hand.
